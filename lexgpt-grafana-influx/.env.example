# Docker Compose - InfluxDB
INFLUXDB_DB=k6
INFLUXDB_ADMIN_ENABLED=true
INFLUXDB_ADMIN_USER=admin
INFLUXDB_ADMIN_PASSWORD=admin123
INFLUXDB_HTTP_AUTH_ENABLED=true

# Docker Compose - Grafana
GF_SECURITY_ADMIN_USER=admin
GF_SECURITY_ADMIN_PASSWORD=Admin@123
GF_UNIFIED_ALERTING_ENABLED=true

# SMTP (optional)
GF_SMTP_ENABLED=false
GF_SMTP_HOST=smtp.gmail.com:587
GF_SMTP_USER=
GF_SMTP_PASSWORD=
GF_SMTP_FROM_ADDRESS=
GF_SMTP_FROM_NAME=Grafana

# Grafana API (for helper scripts)
GRAFANA_URL=http://localhost:3000
GRAFANA_USER=admin
GRAFANA_PASSWORD=Admin@123

# Notification channels (for helper scripts)
SLACK_WEBHOOK=
ALERT_EMAILS=

# k6 runtime (export in your shell when running k6)
# ENV=stg
# INSECURE=true
# BASE_URL=https://user-stg.lexcentra.ai
# EMAIL=
# PASSWORD=
